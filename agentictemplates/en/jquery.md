# jQuery

## Best Practices

- Use event delegation with `.on()` for dynamic elements
- Cache jQuery selectors in variables
- Chain methods for readability
- Prefer `.on()` over deprecated `.click()`, `.bind()`, etc.
- Use `$(document).ready()` for DOM readiness
- Consider vanilla JS for modern browser support
